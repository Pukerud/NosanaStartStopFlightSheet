#!/bin/bash
# This file is intentionally left empty.
# Its presence is required by the HiveOS agent to prevent a "No custom config generator" error.
# No action is needed here as our configuration is handled elsewhere.
